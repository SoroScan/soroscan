name: Playwright E2E

on:
  push:
    branches: [main, master, develop]
  pull_request:
    branches: [main, master, develop]

jobs:
  e2e:
    runs-on: ubuntu-latest
    timeout-minutes: 45
    defaults:
      run:
        working-directory: soroscan-frontend

    steps:
      - uses: actions/checkout@v4

      - name: Install Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 9

      - name: Get pnpm store directory
        shell: bash
        run: |
          echo "STORE_PATH=$(pnpm store path --silent)" >> $GITHUB_ENV

      - name: Setup pnpm cache
        uses: actions/cache@v4
        with:
          path: ${{ env.STORE_PATH }}
          key: ${{ runner.os }}-pnpm-store-${{ hashFiles('**/pnpm-lock.yaml') }}
          restore-keys: |
            ${{ runner.os }}-pnpm-store-

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Install Playwright browsers
        run: pnpm exec playwright install --with-deps chromium firefox

      - name: Build frontend
        run: pnpm run build

      - name: Run Playwright E2E tests
        # Explicit --update-snapshots missing: Playwright forces "none" under CI=true,
        # which fails on first-run linux baselines (win32 snapshots are committed locally).
        run: pnpm exec playwright test --update-snapshots missing
        env:
          CI: true
          # Soften flaky antialiasing diffs once baselines exist in the runner workspace.
          PLAYWRIGHT_BUNDLED_BROWSERS: "1"

      - name: Upload Playwright report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-report
          path: |
            soroscan-frontend/playwright-report/
            soroscan-frontend/test-results/
          retention-days: 14
