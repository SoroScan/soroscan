[package]
name = "soroscan_macros"
version = "0.1.0"
edition = "2021"
authors = ["SoroScan Team"]
description = "Procedural macros for SoroScan event emission"
license = "MIT"

[lib]
proc-macro = true

[dependencies]
proc-macro2 = "1.0"
quote = "1.0"
syn = { version = "2.0", features = ["full"] }
